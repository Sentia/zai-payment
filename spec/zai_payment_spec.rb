# frozen_string_literal: true

RSpec.describe ZaiPayment do
  it 'has a version number' do
    expect(ZaiPayment::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
